{
  "name": "cai-openai-proxy",
  "version": "2.0.0",
  "description": "Character.AI to OpenAI API proxy without puppeteer",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "uuid": "^9.0.1",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
