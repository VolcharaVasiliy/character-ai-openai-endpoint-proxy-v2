{
  "name": "cai-openai-proxy",
  "version": "3.0.0",
  "description": "Character.AI to OpenAI API proxy",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "uuid": "^9.0.1",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
